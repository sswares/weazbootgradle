task installFast(type: NpmTask, group: 'node', description: 'Write more descriptions') {
    args = ['install', 'express', '--save-dev']
}

task removeNodeModules(type: NpmTask, group: 'node') {
    args = ['run', 'removeNodeModules']
}

task updateProjectDependencies(type: NpmTask, group: 'node') {
    args = ['update', '--save']
}

task updateDevDependencies(type: NpmTask, group: 'node') {
    args = ['update', '--save-dev']
}